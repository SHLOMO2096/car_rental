name: Legacy Deploy (Disabled)

on:
  workflow_dispatch:

jobs:
  legacy-note:
    runs-on: ubuntu-latest
    steps:
      - name: Legacy workflow notice
        run: |
          echo "This legacy workflow is intentionally disabled."
          echo "Use .github/workflows/ci.yml and .github/workflows/deploy-prod.yml instead."
